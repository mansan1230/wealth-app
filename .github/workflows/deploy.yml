name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ] # 確保這跟你原本的分支名稱一樣 (main 或 master)

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies 🔧
        run: npm install

      - name: Build 🏗️
        env:
          # 這裡會從 GitHub Secrets 讀取你的 Key 並注入到 App 中
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}
        run: npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 預設打包輸出的資料夾
          branch: gh-pages # 部署到這個分支
